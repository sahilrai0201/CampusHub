{
  "name": "campushub-root",
  "version": "1.0.0",
  "description": "Root orchestrator for CampusHub College Management System",
  "scripts": {
    "dev": "concurrently --kill-others \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
